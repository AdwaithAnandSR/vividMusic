import React, { useEffect, useState } from "react";
import Constants from 'expo-constants';
import axios from 'axios';

const api = Constants.expoConfig.extra.api.dev
export default function useGetGlobalSongs() {
   
   
}
