const handlePlayPause = async ({ track, setIsPlaying }) => {
   try {
      
      console.log(track);
      
      
      console.log(status);
      
   } catch (error) {
      console.error("Error playing sound:", error);
   }
};

export default handlePlayPause;
